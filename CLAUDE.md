# v2-hub private notes

Implement only `engine/hub/` and focused tests. Keep the service Docker-free, loopback-only, and rooted in an explicitly supplied v2 data directory. Never use live v1 data, external mail, providers, or the v1 UI/public listener.
