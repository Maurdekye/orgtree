# Orgtree v2 integrated acceptance

Updated 2026-09-08 00:01 UTC. This is an independent evidence record, not a declaration of MVP completion. Later dated results supersede historical findings below. Binding scope is `v2-user-decisions.md` (newest decision wins), the retained requirements in `v2-original-design-brief.md`, and `v1-parity-inventory.md`. Technical wiring is described in `engine-contract.md`.

## Evidence levels and commands

- **Source** means inspected code only; presence does not establish functioning behavior.
- **Fixture** means a deliberately restricted test double or seeded state; it is not a provider success or full interaction demonstration.
- **Real application** means the actual compiled main entrypoint, preload, copied renderer and Python engine run together with isolated data. The acceptance driver instruments Electron to observe windows and drive its DOM. It acknowledges native message boxes and reports error dialogs as failures.
- **Installer** requires running the distributed executable through installation and launch. A development Electron pass is not installer evidence.
- **INERT** means prerequisites were unavailable and no behavioral verdict was possible. It exits 2; FAIL exits 1 and PASS exits 0.

Run the acceptance instrument's positive/negative controls with `node --test tests/acceptance/harness.test.mjs`. Run the assembled app with `node tests/acceptance/run.mjs`. Optional absolute environment variables select `ORGTREE_ACCEPTANCE_APP`, `ORGTREE_ACCEPTANCE_ELECTRON`, and `ORGTREE_ACCEPTANCE_PYTHON`; defaults select the current checkout and its bundled dependencies/runtime. Build the chosen app first using its normal build command.

The runner creates a fresh `orgtree-v2-acceptance-*` directory under the OS temporary directory, outside the live v1 tree. It sets both data selectors and an isolated Electron profile **before any application import**. The inherited `ORGTREE_DATA` points to a separate empty `inherited-v1` fixture, which the shell treats as forbidden; the shell must replace it with the intended `ORGTREE_V2_DATA` root in the child. Both launches use that same fresh data/profile. It retains the data and structured `report.json`; it does not recursively delete anything. Raw application output is not printed. No acceptance case attaches to an existing engine or port 7360. The driver stops before UI mutation if the real root/PID/port handshake fails, and reports whether its observed child processes exited.

## Current results

At baseline `696c160`, engine launcher/backend/runtime and build artifacts were absent. The real application runner returned **INERT** with every missing component named. Both acceptance-instrument tests passed: an empty fixture finds all seven missing prerequisites, a complete fixture enables the preflight, removing Python disables it, and separate runs get fresh empty roots. This establishes only that the instrument can reject missing prerequisites, not that the app works.

The first real startup exposed the bundled interpreter's Windows `_pth` path `../..` resolving to `engine` rather than the package root. Shell owner independently reproduced and fixed it with `../../`; engine owner also added explicit package-root insertion. This startup objection is resolved in the tested later build.

The basic whole-app run against assembled shell `2342c2e` passed two launches: exact engine root/PID/port, nonempty real React renderer, bridge/default preferences, authenticated API and unauthenticated rejection, close-to-tray with live engine and reopen, and seeded draft text/attachment/reply storage across a full engine restart. Both observed Python children exited. Retained evidence: `C:/Users/ncola_k8bx/AppData/Local/Temp/orgtree-v2-acceptance-5VSQ00/report.json`. This proves storage transport with seeded values, not an actual composer send/reply workflow.

The expanded real UI run on the same build found a **blocking integration defect**. Creating `Acceptance Runtime` through the visible form succeeds durably, then selection pushes the URL to `/o/acceptance-runtime`. The shell's `trustedUiUrl` accepts only `/` and `/index.html`, so it strips engine credentials and rejects native IPC on the selected-org route. Subsequent Chromium API requests return 401, WebSocket fails, and Settings cannot open. A fresh launch at the home route restores access and shows the created organization; actual Settings modal popout/redock and keeping that child open when the main closes pass from that home route. Evidence: `C:/Users/ncola_k8bx/AppData/Local/Temp/orgtree-v2-acceptance-rgGW6V/report.json`. Shell owner and coordinator received the route mismatch and exact reproduction. The runner now includes an explicit selected-org route check on both launches; this defect remains open pending an updated build.

**Correction and rerun:** shell `158f545` fixed the route boundary, included in the tested acceptance checkout `80c6abc`. Both launches now pass actual UI creation/selection, exact selected-org REST and native access, real WS, actual Settings modal popout/redock, main close while that child survives, and stored draft continuity. The earlier routing objection is withdrawn. Two initial test failures after that fix were invalid selectors assuming the home navigation stayed mounted; the corrected driver uses the active org header and opens the real organization drawer.

**Historical shutdown failure (corrected below):** in the expanded run `C:/Users/ncola_k8bx/AppData/Local/Temp/orgtree-v2-acceptance-vHZQUl`, both phases completed all UI assertions and both Python children exited, but the second Electron process remained until the parent runner's timeout. Its old aggregate `PASS` is **invalid**: the runner failed to fold a second-phase timeout into the overall result. That acceptance-instrument defect is fixed and covered by a regression proving timeout, nonzero exit and surviving child each force FAIL even when every UI assertion passes. Do not cite this run as successful full shutdown. The shell owner has the observation; unload behavior is a hypothesis pending a corrected rerun. The three instrument-control tests pass.

**Shutdown correction independently verified:** at 21:39 UTC, the shell owner's actual built private worktree (`707670b` base **plus its uncommitted explicit-Quit correction**) passed the entire sequence twice with the corrected runner. Every UI assertion passed; both Electron processes returned `processExitCode: 0`, `processError: null`, and both observed Python children exited. Evidence: `C:/Users/ncola_k8bx/AppData/Local/Temp/orgtree-v2-acceptance-5F4DZE/report.json`. The correction allows explicit quit to override a renderer unload blocker, while normal user-close behavior is retained. The shutdown objection is withdrawn for this tested build; do not attribute the result to base commit `707670b` alone. This remains development Electron with the real bundled runtime, not installer acceptance.

**Independent MCP authorization gate and one real provider turn:** after coordinator authorization at 21:40 UTC, `verify_mcp.py` ran both landed HTTP/MCP cases using the actual bundled Python interpreter. The fixture seeds identities and suppresses provider discovery, but retains real launcher startup, HTTP authorization and a real `orgtree.mcptool._post` subprocess. Exact isolated root/non-7360 port, valid tool success, missing/forged credentials, forged node/org, stale generation, agent-to-desktop denial and stripped owner secrets all passed. Evidence: `C:/Users/ncola_k8bx/AppData/Local/Temp/orgtree-v2-mcp-acceptance-dtnd0j26/report.json`.

The cleared gate was followed by exactly **one real Codex Luna agent**, in a new root `C:/Users/ncola_k8bx/AppData/Local/Temp/orgtree-v2-provider-debai1vb`, source `a3e6ab0` including engine authorization `d7ce9e0`, using the bundled interpreter. Real Codex process configuration and its verified generation-0 scoped credential targeted port **53696**, organization `v2-codex-acceptance`, node `probe`; its working directory was inside this fresh data root. One actual `orgtree_chart` HTTP 200 request was observed. The actual assistant returned `V2_ACCEPTANCE_RESULT 6 times 7 = 42; organization=v2-codex-acceptance; node=probe; chart confirmed.` The durable turn receipt records lane `codex`, tier `luna`, route `reserve`, outcome `completed`, and `warm: false`; cost remains unknown. Engine PID 36296 exited with code 0, and an independent Windows process-tree query found zero remaining descendants. Evidence: that root's `report.json`, `mcp-target.json`, `tool-observations.jsonl`, and `data/turnlog/v2-codex-acceptance/probe/1788817603297-0002-codex-completed.json`.

The real-provider driver keeps the actual launcher, provider process specification and tool result. Its test-only observation wrapper refuses agent tools other than the one authorized read-only chart and rejects any wrong origin, org or node before sending HTTP. It does not synthesize provider output. Existing harness sign-in is used without deliberately editing credential/configuration files. `run_provider.py` is **INERT** by default; `--authorize-one-codex-turn` is a technical switch, not permission to repeat this spent authorization. No second agent, second task, interrupt/resume or crash recovery was exercised. The isolated organization and transcript remain retained.

## Packaged components and visual evidence, 22:12 UTC

`ORGTREE_ACCEPTANCE_PACKAGE=<absolute win-unpacked>` runs the actual packaged app archive, preload, renderer and bundled engine inside the development Electron driver. It captures login registration calls without modifying Windows and blocks updater network. The two-launch engineering package run `C:/Users/ncola_k8bx/AppData/Local/Temp/orgtree-v2-acceptance-U47hAB/report.json` passed with both Electron exits 0 and no remaining Python children. Its recorded checkout SHA belongs to the driver, **not the package**; that early package had no fixed provenance. This is not NSIS install/uninstall evidence.

The shell owner subsequently produced a fixed-source engineering installer at commit `b7f0ec4d99bf5ee4699ea1be37520057d6020fac`, SHA256 `5b9e977844c8739e03d77c1e8def133acf0ce4c45b499be730036d6910a93c1e`. Actual per-user install/uninstall is authorized only in a fresh test directory after refusing any existing registration/shortcut collision; at that checkpoint it had not run. The actual completed test is recorded below. Later engine/renderer fixes require another release build.

`ORGTREE_ACCEPTANCE_VISUAL_FIXTURE=1` adds three idle synthetic nodes and a markdown document through the real ledger; provider discovery/warming is suppressed and a positively tested subprocess barrier rejects provider execution. Only the real hub's `whoami` and isolated-readiness-file `icacls` utilities are allowed. This mode cannot establish provider execution or populated conversation behavior. It supports source builds only. The normal mode remains the unmodified launcher.

Actual Electron screenshots are retained under `C:/Users/ncola_k8bx/AppData/Local/Temp/orgtree-v2-acceptance-EzF0Wj/`: `initial-organization.png`, `initial-settings-display.png`, `initial-settings-import.png`, `initial-settings-runtime.png`, `initial-settings-popout.png`, `initial-document-reader.png`, `initial-connections.png`, `initial-agent-context-menu.png`, and `initial-agent-desk.png`. The coordinator personally reviewed the images. Main capture uses Electron; adopted native windows require Chromium's real `Page.captureScreenshot(fromSurface:false)` fallback because Electron returned `UnknownVizError`.

**The screenshot run's PASS is not visual acceptance.** It verified interactions and image capture but revealed two product defects: (1) two explicit Fit actions still leave both child cards mostly below the canvas; (2) native Settings loses its theme and displays default white/serif HTML. Owners have the failure images. The focused agent desk opens, but its conversation is empty synthetic data. The Connections capture shows the older scoped pairing form, not the newly requested complete Host/Connect flow.

Measured evidence is `C:/Users/ncola_k8bx/AppData/Local/Temp/orgtree-v2-acceptance-9DHJS6/initial-geometry.json` and `initial-popout-styles.json`. Viewport bottom is 847px; child nodes begin at about820px and end950px after settled Fit. The popout stylesheet incorrectly resolves to `/o/assets/index-CBd61at6.css` after organization route selection; its body remains Times New Roman. A sheet-count assertion was insufficient because the popout has an inline sheet; the current driver instead compares the actual themed main panel's font/color/background to the adopted panel.

Those early screenshot runs used an owner's changing worktree, so their end-of-run checkout SHA is not immutable bundle provenance. New runs save hashes of engine source, compiled resources and bundled interpreter before launch, capture build-info separately, and fail if those bytes change during the two phases. Remaining screenshots/behavior include real replies and previews, populated conversation, overlap behavior, History pagination/popout downloads, import preview/copy/recovery and full Host/Connect.

### Verified corrections and actual installer, 22:25 UTC

The native stylesheet defect is **fixed and independently verified**. A clean private build at `07222960d9828d3bc70ad9a25a92ad4f9973cd80` passed both launch phases with identical main/native panel background, color and font, actual transfer/redock, screenshots and clean process shutdown. Its build-info commit matches the source and runtime digest `6ce12c91acbf2748983e0f860734c03cb3c4166ef75b95bcfaa55c64a26186d0` remained unchanged. Before image: `EzF0Wj/initial-settings-popout.png`; after: `C:/Users/ncola_k8bx/AppData/Local/Temp/orgtree-v2-acceptance-kFnpt6/initial-settings-popout.png`. The fix copies the already-loaded stylesheet URL rather than resolving a relative URL after the organization route changes.

The graph observation is narrower than the earlier report: initial captures genuinely clip the children, but optional `?.click()` did not positively prove the Fit button was present. The renderer owner identified a late org-inbox layout that adds160 world pixels of headroom. The next check must wait for that node, assert the real Fit control and compare before/after geometry before attributing a failure to explicit Fit.

**Actual NSIS install, installed executable and uninstall now pass for the engineering artifact.** Evidence directory: `C:/Users/ncola_k8bx/AppData/Local/Temp/orgtree-v2-install-1e9bcdd2e0604d8380637e310c1f4c52`. The exact `b7f0ec4` artifact and SHA above were checked; no existing installer GUID, matching shortcut or login registration existed. Per-user installation into the fresh directory exited0. The real installed `Orgtree.exe` launched using a separately proven `--user-data-dir` profile and explicit fresh V2 data. Its actual renderer/native bridge and authenticated API worked, unauthenticated API returned401, its engine port matched the new data file and differed from7360, and the UI created an empty `Installer Acceptance` organization. Native Quit exited0. `installed-app.png` captures this installed application, not a development executable.

Default login created `HKCU Run/com.maurdekye.orgtree` pointing at the exact test executable with `--background`. Both expected shortcuts and both expected HKCU installation keys were recorded. Only the test installation was uninstalled after checking its canonical location and absence of reparse points; uninstall exited0. Any unchanged test-owned login value was removed. Final registration, shortcut and login inventories are empty, the executable is gone, and all8 isolated data files remained byte-identical. Profile/evidence/data remain retained. No provider turns ran. This proves the recorded engineering install path, not the later final package, all-users installation, update execution or complete MVP parity.

### Second real provider path, 22:36 UTC

The coordinator authorized one Claude subscription task and then one replacement after a test configuration error. First attempt `C:/Users/ncola_k8bx/AppData/Local/Temp/orgtree-v2-provider-ytsn3_36` used the Codex fixture's `plan` mode. Real Haiku computed42 but Claude Code refused even the read-only chart in plan mode before HTTP. Its completed provider receipt and zero chart requests remain retained; this is not evidence of a transport failure. Engine14272 exited0 and later independent process inventory found no descendants or matching processes.

The explicitly authorized replacement used a new root, `C:/Users/ncola_k8bx/AppData/Local/Temp/orgtree-v2-provider-wxs6eviy`, with `acceptEdits` but bash/edit/web/subagent switches disabled and a chart-only transport observer/guard. Actual Claude CLI auth status was `loggedIn:true`, `authMethod:claude.ai`, `subscriptionType:max`. Source `82ee0335b5062fc4b54e43c125415a201725241d`, bundled Python, engine port51828, organization `v2-claude-acceptance`, node `probe`, generation0. Actual CLI MCP configuration used the bundled interpreter, exact engine port and a verified scoped token. The observed provider spawn had no injected API key or alternate account token and its cwd was inside the test root.

**Replacement PASS:** one actual chart HTTP200 and assistant `V2_ACCEPTANCE_RESULT 42 organization=v2-claude-acceptance node=probe`. Durable receipt `data/turnlog/v2-claude-acceptance/probe/1788820473323-0002-claude-completed.json` records laneclaude, tierhaiku, outcomecompleted, warmfalse and a successful result. Its route field is null; subscription attribution comes from the separately observed CLI auth method and spawn environment, not an invented receipt route. Engine42264 exited0; independent process inventory found zero descendants/matching processes. `report.json`, `mcp-target.json`, `provider-spawn.json`, `tool-observations.jsonl` and `cleanup-check.json` retain the evidence. Neither attempt is authorization for another turn.

Sequencing limitation disclosed to the coordinator: the first engine's exit0 was known before the replacement, but an independent PowerShell descendant verifier stalled on an empty-array loop and the replacement was launched before that verifier returned. The corrected verifier later confirmed no first-root descendants; the replacement's cleanup verifier completed normally. No overlapping provider process was observed, but the stricter requested pre-launch independent receipt was not obtained on time.

## Whole-app feature matrix

| Requirement | Acceptance boundary | Current evidence / remaining case |
|---|---|---|
| Fresh first launch, separate v2 root, copied Python source and real interpreter | Main spawns actual bundled interpreter; readiness root/PID/port must match; nonempty renderer must mount | Real application PASS on both launches of `2342c2e` |
| HTTP assets, REST, WebSocket and native bridge share the engine | Actual renderer loads; authenticated API succeeds and unauthenticated request fails | Real home and selected-org REST/WS/native access PASS after `158f545` |
| Organization create/select/settings/default project folder | Create isolated organization from visible UI, reload and verify persistence/default grant | Actual create + restart persistence + selected-org authority PASS; default folder interaction pending |
| Graph, desks, eye switchboard, capacity and lifecycle | Actual isolated org/agent graph and live conversation | One real API-created Luna agent and durable assistant conversation verified; graph/desk/eye/capacity interactions still pending |
| Main close leaves engine and popouts; explicit Exit closes everything | Close real main, prove engine API still responds, reopen; repeat with actual desk/modal popout | Main close/reopen, actual Settings modal retention/redock, and explicit application quit PASS with shell's corrected private build; both Electron and Python exit cleanly. Clicking tray/toolbar Exit and installer behavior remain separate checks |
| Popout transfer, redock, reload and monitor layout | Same actual composer node/text/attachments/reply; restored windows on manual reopen | Actual same-composer native transfer/redock PASS; actual quiet second launch then manual show restores desk and Settings with exact saved bounds, source, typed unsent text and uploaded attachment chip (exBuKk). Multiple monitors and all-modal restoration remain unverified |
| Draft and event reply persistence | Draft text/attachment/reply storage survives two full engine launches; actual source identity/preview/send receipt survives reload/compaction | Actual context Reply/preview/send/canonical pending receipt/manual quote removal PASS FJchV0. Actual typed unsent text, source event identity and quote, plus real upload attachment chip survive quit, new engine and quiet/manual restoration exBuKk. Provider compaction continuity remains separate |
| Pinned overlap fading, opacity/toggle and contextual actions | Open overlapping real pinned surfaces; exercise toggle, slider and right-click operations | Actual pinned Settings overlaps focused desk: default opacity0.7, slider0.45, toggle off1/on0.45, context-menu Unpin PASS exBuKk. Actual user UI and scoped superior Unstick pass SSpjHi with retained replay/admission-once and self/peer refusal; broad context-action parity remains separate |
| Mail, questions, credits/scope requests, docket | Exact identities, reply target, successful submission and durable refresh | Copied source only; integrated cases pending |
| Documents, HTML and ZIP downloads | Actual Download action saves usable source/filename; ZIP includes local assets; downloaded content has no bridge | Actual main/adopted Markdown source and History HTML downloads preserve source bytes/server filenames. Actual present?ZIP retains original index/CSS/SVG after source edit and excludes unrelated file kYszoY. Restricted viewer has no bridge/API authority; preview local assets absent and internet blocked by retained v1 CSP (open requirement gap) |
| Peer Connections and existing Mail views | Manually host a hub on one isolated installation and connect another; send/acknowledge local fixture correspondence and reconnect | Two separate actual Electron/engine roots pass Host settings, scoped credential issuance, manual Connect form, rejected credential/no persisted connection, sanitized status and two-way local mail received once. Settled Connected/online peer visible. WAN/TLS reachability, explicit transport ACK, restart/reconnect and Mail composer UI remain unverified |
| Provider/harness setup, auth and usage | Missing harness visible, detect-only links; configured connection identity and honest availability | Shell detection fixture previously reported by owner; real UI/provider evidence pending |
| Real agent turn + MCP identity | ONE isolated Codex Luna/Astra arithmetic task and read-only chart, verifying MCP targets same test engine | PASS after independent MCP gate; actual Luna reserve-route turn, arithmetic42 and one exact-org chart, clean engine/process-tree shutdown. Single-turn authorization consumed |
| Interrupt/resume and crash recovery | Preserve existing session; active agents resume; idle queued agents stay idle; uncertain tool receipts reconcile | No further provider turns authorized. Native relaunch and no-provider engine recovery controls pass separately; full crash/import/provider session continuity remains with engine/import owners |
| Copy-only import and enabled automation recovery | Import a synthetic v1 root; prove source bytes unchanged; active/idle/automation distinctions | Actual Settings preview/ack/copy plus imported active/idle chat API and rendered document/history PASS with synthetic source, active-only admission recorder and unchanged source hashes. Uncertain recovery and actual resumed worker/automation execution remain unverified |
| Retained authority, generations, storage, diagnostics and automation | UI and agent tools reach same ledger; no generation/draft reuse; SQLite/history survives restart | Actual History UI pages115 read-mail and105 each current/prior-generation/retired transcripts and documents, asserting every displayed row at tied timestamps. Retained authority/diagnostics/automation and provider continuity still require their own cases |
| Needs-attention notifications | Defaults only question/urgent mail/ticket attention; open exact target; optional routine notifications | Renderer owner reports actual engine?renderer?native IPC?exact click/default/routine-policy PASS OZ2t0e with OS toast display/emit instrumented. Question target prefix fixed f367cf2. Physical OS notification display remains unverified |
| Tray statistics, quiet login, no sleep prevention | Actual count changes and quiet startup; close/reopen; installed login setting | Actual installed default login registration/removal PASS engineering b7f0ec4; typed runtime stats PASS after21fb0ad. Actual --background second launch keeps only hidden main and live API; manual show restores popouts exBuKk. Physical login/sleep behavior and tray clicks remain unverified |
| Automatic prerelease update at safe idle point | Active work blocks install; unavailable/stale status blocks install; real downloaded update applies when idle | Real agent request?native polling?exact acknowledgment?two actual native relaunch cycles PASS. OS-active guard, update-noop admission preservation and throwing native action/failure release PASS. Updater result/OS idle controlled; no real downloaded update or installer application |
| Current-user/default or all-users Windows installer, uninstall retains data | Install packaged artifact, launch bundled engine, uninstall and verify preserved org/history | Actual current-user b7f0ec4 engineering installer/installed UI/uninstall PASS;8testdatafiles retained byte-for-byte and test registrations removed. All-users and final-source installer remain unverified; unsigned prototype accepted by user |
| Provider-neutral themes and honest capability differences | Theme changes preserve authority/routing; status remains identifiable without color; two materially different adapters | Actual Codex Luna reserve and Claude Haiku subscription tasks each completed arithmetic and scoped chart; native neutral/OpenRouter selector and persisted preferences PASS with actual Settings screenshots; graph/desk variant capture still expanding |

## Integrated milestones at 23:00 UTC

- **Desktop route correction:** actual built UI at47909a3 returned SPA HTML200 for desktop status, making native maintenance inert despite successful agent requests. Evidence `Temp/orgtree-v2-acceptance-NIKVpP`. Engine21fb0ad moves desktop routes ahead of the fallback. Typed JSON/schema and real native polling now pass. Earlier HTTP200-only status checks are insufficient.
- **Native maintenance normal path:** `Temp/orgtree-v2-acceptance-IECRg0/report.json`, source21fb0ad, runtime digest275509accfaf9acd4874ad3656603f6f96d0112824316e3dfdaa6e6e56571596. Scoped actor controls, OS-active no-ack, up-to-date acknowledgment with admission open, wrong-ID refusal and exact execution acknowledgment pass. Relaunch was captured. Build-info dirty=true from acceptance changes; immutable runtime bytes are not a clean release claim. An earlier external-package updater stub observed the wrong instance; the corrected instrument observes the actual bundled updater.
- **Native failure:** `Temp/orgtree-v2-acceptance-e6Dp22/report.json`, clean frozen shell34e1c5e. Controlled throwing native restart reaches the real exact-ID failure endpoint, records failed, releases only its admission hold, keeps chart available and never retries execution on the next native poll. Native failure queue clears while automatic updates remain blocked. App/engine exit0.
- **Two actual native relaunches:** `Temp/orgtree-v2-acceptance-SWrxkN/report.json`, source21ec997, dirty=true acceptance workspace, stable runtime digest64e1894535bbae7f0ebcac00db630431b061a734988200deb67f8f0636d5ab18. Unmodified app.relaunch starts Electron39128?37456?4364 and engines2596?12272?21136 at the same isolated root/port61134. Prior acknowledgments become honest unknown outcomes and a deliberate new scoped request is allowed each boot. Third request is cancelled. Three clean engine exits and zero remaining Electron/engine/guardian PIDs. No provider turn or installer update.
- **Import and themes:** actual Settings preview, required acknowledgment, copy and real organization selection pass at21fb0ad in `Temp/orgtree-v2-acceptance-XCfpRt`. Six synthetic source files are unchanged; active admission recorder called once, idle never called. Actual imported document and copied conversation screenshots: `initial-imported-document.png`, `initial-imported-history-desk.png`. Provider processes and watchdog delivery are disabled; this does not certify uncertain recovery or provider continuity. Neutral/OpenRouter selector, persisted native preference and unchanged provider/status labels pass; screenshots in the same root. The full run fails only because its older restart expectation assumed the original organization instead of the correctly restored imported one; driver correction is underway.

## Deliberate exclusions and boundaries

### Native scheduling and actual History,23:28UTC

The standards-only candidate d5bf8cf changed the native document to CSS1Compat but did not change its hidden-document/RAF stall or serif-looking screenshot. Adding the separate shell scheduling candidate f55e5d1 produced clean tested combined commit ee3150a, runtime digest `9766c0987fc863fca3b910fea60c55b196c6cdbf78934d77eeeb9a4e4caf448f`. Full two-launch run `Temp/orgtree-v2-acceptance-Xly6ps` passes with native visibility now visible and both animation frames completed. The settled glyph appearance remains unresolved. These are isolated candidate builds, not final release provenance; neither change is claimed to fix the glyph issue.

`node tests/acceptance/run_history.mjs` creates a fresh isolated synthetic SQLite fixture through production writers, then runs the actual bundled launcher and Electron renderer with provider processes disabled. Clean ee3150a run `Temp/orgtree-v2-acceptance-zA5rRw/report.json` passes: read-mail115 records in50/50/15 pages; current, prior-generation and retired transcripts105 each in50/50/5; documents105 in50/50/5. Every displayed row, first/last boundary and disabled final Older control is asserted, with all timestamps deliberately tied. Actual native History document Download saves exact original `DOC-104.md` bytes. Redocking works and the gallery displays100 then5 exact oldest documents. Electron exits0, observed engine exits, and runtime bytes remain unchanged. Fixture seeding is not a real provider turn or session-resume proof.

Representative screenshots under that same absolute Temp root: `CURRENT-page-2.png`, `history-native-document-download.png`, `gallery-older-five.png`. Main History and gallery are readable; native History shares the unresolved typography observation. Earlier instrument runs failed on selector quoting, an unclonable DOM-presence return, and consecutive Newer clicks before React committed the first page; corrected bounded waits produced the recorded passing run.

### Two actual local installations,23:36UTC

`node tests/acceptance/run_connections.mjs` starts two independent Electron/engine instances with fresh roots/profiles and provider execution disabled. Clean frozen ee3150a run `C:/Users/ncola_k8bx/AppData/Local/Temp/v2-local-connections-hGxQ10/report.json` passes at engine ports53015/53014. Actual Host controls save loopback listen/advertised host and dynamic port; the host issues a scoped credential through its UI. A deliberately invalid credential returns502 and persists no client connection. The actual client manual Connect form then succeeds, and its sanitized API omits the credential. Each engine sends one synthetic local message through the normal org-inbox API and receives the other's message exactly once. Settled Connections UI reports Connected and the remote organization Online. Both Electron exits are0 and observed engines24512/13360 are gone; runtime digest9766c098 remains unchanged.

Host screenshot: `Temp/orgtree-v2-acceptance-OgfWQP/host-mail-delivered.png`; client: `Temp/orgtree-v2-acceptance-0VCCbu/client-mail-delivered.png`. Scoped credentials are hidden before capture; their private exchange file stays only in the isolated fixture. This is local transport and real Host/Connect UI evidence. No external party, provider, WAN/TLS reachability, explicit transport ACK, reconnect cycle or Mail composer UI is claimed. Early instrument field assumptions (`slug` versus `identity.slug`, `direction` versus `dir`) were corrected before the passing run.

Renderer-owner follow-up at23:35UTC reports actual platform-font and width evidence in its isolatedCia149 probe: Segoe UI on main/child/fresh control,29 history glyphs and46 composer glyphs, identical scale-corrected text width. Multiple capture methods are byte-identical. The earlier visual appearance does not establish actual font substitution, and no additional CSS change is justified by that observation. This is independently supplied diagnostic evidence; the coordinator retains visual review.

### Reply, source download and recovery evidence at 23:15 UTC

`Temp/orgtree-v2-acceptance-9SC4Lk` uses immutable runtime21ec997 (dirty acceptance workspace). Actual imported-history context-menu Reply, selected source preview, typed text, same-DOM native desk transfer/redock and Send all ran. Canonical SQLite/inbox reply context matches the selected source exactly. The actual document Download saved `Copied-Import-Report.md`,66 bytes identical to its source, through Electron's native download event. Quote-removal Cancel preserved the source; confirming removal made an old reference return422. No provider ran.

That run remains **FAIL** because chat `pending_mail` omitted canonical `reply_to`, even though the ledger retained it. Engine55384a1 fixes that projection; actual UI rerun is pending. Native desk screenshots also appear serif although computed message/composer font families match the main window. This remains a visual discrepancy under investigation, not an established font-rule diagnosis. Settings styling alone is insufficient to certify all adopted content.

**Rerun correction,23:17UTC:** `Temp/orgtree-v2-acceptance-FJchV0/report.json` passes both actual launch phases on55384a1 with unchanged runtime digest `ee2a71199ae6f66de16b63eb7355c5f177034d9974db353b5edbe708ba03ff6c`; build-info dirty=true reflects acceptance changes. Pending reply projection now contains the canonical source, and the objection is withdrawn. Both Electron exits are0 and observed Python children are gone. Source download, real Reply/send/redock, quote-removal controls and import/restart all pass their bounded assertions.

Visual signoff remains open: `initial-reply-popout-settled.png` still appears serif. `reply-popout-styles.json` records identical computed fonts/sizes/variables throughout the main and child ancestor chains, but main is CSS1Compat and child BackCompat. The child reports native visible/not minimized while its document reports hidden. Font readiness resolves; two animation frames do not complete within1.5seconds and that diagnostic is explicitly INERT. The precedingPu0PhE run used an unbounded diagnostic wait and hit the overall deadline; it is retained as an instrument failure, not a successful Reply run.

Independent exact exported recovery3339a21 passed8 engine checks in separate isolated processes: recovery resolution2, import integration3 and actual HTTP3. Held-worker, stale attempt, explicit settlement, source-history and scoped-route controls are included. These are bounded engine fixtures; native provider continuity and whole recovery acceptance remain separate.

The corrected restart driver now accepts the actually restored imported organization before switching through the real selector. Its restart assertions pass in9SC4Lk; the earlierXCfpRt fixture expectation is corrected. The fresh-start fit result remains independently valid. Current actual Reply persistence coverage is transfer/redock; two-launch draft storage uses explicitly seeded values, so unsent composer/source restoration after restart is still unverified.

New runnable cases: `node tests/acceptance/run_maintenance.mjs` covers native acknowledgment with controlled OS idle/update results and captured relaunch; `ORGTREE_ACCEPTANCE_MAINTENANCE_FAILURE=1` adds a throwing action and exact failure release. `node tests/acceptance/run_relaunch.mjs` performs two actual isolated native relaunches, then cancels a third deliberate request and quits. Neither executes providers or a downloaded installer. `ORGTREE_ACCEPTANCE_VISUAL_FIXTURE=1` plus `ORGTREE_ACCEPTANCE_IMPORT_FIXTURE=1` enables synthetic source import/Reply/download in the normal app runner; it disables provider execution and records admission instead.

MVP excludes kiosk/public browser access, mobile, all agent execution sandbox/isolation, Git management/verification, and all multi-account registration/balancing/reassignment/fallback. Renderer isolation and folder/tool authority remain required. The 21:22 decision explicitly retains manually exposed authenticated mail-hub transport, which does not restore browser/API sharing. These exclusions override older inventory rows; they are not failures. The 21:18 phase gate forbids approaching, designing or implementing post-MVP work until verified full MVP completion; the conditional later sequence is Git, symmetric accounts, then mobile, within the same hard stop.

The acceptance harness never uses a real provider by default. The coordinator's MCP HOLD was cleared only after the independently verified controls described above, and the authorized single arithmetic/chart turn has now succeeded. One optional interrupt/resume case is permitted after that success but has not been run. No additional provider run is implied by adding later UI/import/download checks. Fixtures cannot establish provider execution, session continuity or uncertain mutation recovery.

Hard stop: 2026-09-08 01:00 UTC or usage exhaustion, whichever comes first. Incomplete rows remain incomplete at the checkpoint.

Fresh-start fit correction: immutable82ee033 runtime (includes c45629d), evidence Temp/orgtree-v2-acceptance-4R0KYf, independently fits all five rendered cards before and after an asserted real Fit-button click. The entire run is FAIL because its restart phase restores the previously focused desk and violates the new whole-graph precondition; fresh-start fit and restart storage/style/shutdown results are recorded separately.

## Latest bounded runtime results, 8 September 00:01 UTC

**HTML source and ZIP:** `C:/Users/ncola_k8bx/AppData/Local/Temp/orgtree-v2-acceptance-kYszoY/report.json` passes on clean built f367cf28a8d3d91ede4727c0ffa1a8332fbd620f, unchanged runtime digest `75e1e0be9a132d04bb9994b9f3580056faa2093f6b44e7a1934b5bf706ed9962`. Real scoped synthetic actor presents standalone and locally bundled HTML through the actual engine. History Download saves original `Standalone-HTML.html` and `Bundled-HTML.zip`; the ZIP contains exactly index.html, assets/site.css and assets/mark.svg with original bytes despite later source edits. Unrelated files are excluded. Actual restricted artifact viewer has no desktop bridge and cannot fetch operator API; the main frame succeeds as positive control. Electron exits0 and engine17988 is gone. Run with `node tests/acceptance/run_artifacts.mjs`.

This is not full preview acceptance: personally viewed `restricted-html-viewer.png` lacks linked local CSS/SVG. The actual inherited `_MOCKUP_CSP` also prohibits internet resources, contrary to the explicit20:43 decision. Core/download owners and coordinator have the finding; fix and a bounded network/rendering rerun remain required. `html-and-zip-history.png` and `zip-proof.json` retain the successful download evidence.

**Actual quiet restart and unsent Reply:** `C:/Users/ncola_k8bx/AppData/Local/Temp/orgtree-v2-acceptance-exBuKk/report.json` passes both phases on the same clean f367cf2 build/runtime. A production-writer synthetic assistant journal is used; no provider executes. Real context-menu Reply selects its immutable event, real composer typing stores an unsent message, and a synthetic File dropped onto the desk is uploaded through the real API. Desk and Settings move to native windows before explicit quit. A new engine PID starts at the same isolated port57424; `--background` produces exactly one hidden main window and no restored child windows while the authenticated API responds200. Manual native Show restores both windows at exactly saved bounds, with original source event/quote, typed text and uploaded attachment chip intact. Closing main leaves both popouts and engine alive. Both Electron exits0; engines25092/39152 are gone. Run with `node tests/acceptance/run_lifecycle.mjs`.

The same real interaction verifies pinned Settings physically overlaps the focused desk, opacity0.7 by default, slider adjustment to0.45, toggle off to1 and reenable to0.45, then actual title-bar context-menu Unpin. Screenshots personally viewed: `quiet-restored-reply-attachment.png`, `initial-overlap-default.png`, `initial-pinned-context-menu.png`; `initial-overlap-disabled.png` is the opaque positive control. This does not certify every context action, multiple monitors, provider restart continuity or physical OS login. InitialCk41F9 driver wrongly searched for the staged attachment inside the composer; the actual attachment row was visibly present. Corrected selector produced the reported pass.

**Typography correction:** coordinator withdrew the serif-font defect diagnosis at23:36 after independently supplied actual platform-font/width and identical capture controls. Earlier typography observations above are historical, not an open CSS defect. No further typography probes are required.

**Latest ownership:** actual Settings Recovery/native import UI is owned by v2-interface with v2-import's native-ready fixture. Earlier display-only import fixture evidence does not establish native continuity under the newer admission gate. Native notification routing/defaults were independently verified by that owner with OS toast emission instrumented. Final source/package acceptance remains open; engineering b7f0ec4 installation is not final-package evidence.

## Consolidated package and supervised Unstick, 00:06 UTC

`C:/Users/ncola_k8bx/AppData/Local/Temp/orgtree-v2-acceptance-3jVgAo/report.json` passes both launches of the exact clean36d8aab packaged resources, runtime digest `9643004c7130a92795f7e32f22d2436e0f180a7539279be6776c0ba2ff948e8f` unchanged. Actual packaged ASAR/main/preload/UI and bundled Python handle org creation/selection, authenticated REST/WS, Settings native styling/transfer/redock, main close/reopen, seeded draft storage and explicit quit. Both Electron exits0 and engines23612/34764 are gone. Login registration is captured without changing Windows, updater network blocked, and the host executable remains development Electron: this is packaged-component acceptance, not a new NSIS installation or current final release. Package36d8aab predates the pending preview resource correction.

`node tests/acceptance/run_unstick.mjs` passes at `Temp/orgtree-v2-acceptance-SSpjHi/report.json`, source84345d3 with dirty acceptance helpers, unchanged runtime digest `248f81530a0d9a2d6fc78a695193e80d38dc1456131e58be7ce9a0e00b39e264`. Actual frozen builder desk Unstick invokes the real operator route, clears its locks and admits exactly its retained text and view. An authenticated scoped superior then invokes real agent Unstick on reviewer, clearing frozen/limit/last-holder global lock and preserving retained text/view and sender. Self and peer attempts return422 with zero admission, and repeating release is a no-op with no duplicate admission. Only send_message execution is replaced by a recorder, so no provider resumed and no provider continuity is claimed. Actual engine41556 is gone and Electron exits0. Screenshots `frozen-builder-before-unstick.png` and `user-unstick-after.png` show the bounded UI; the frozen screenshot was personally viewed. InitialufNyGd used an invalid boolean synthetic global lock; correcting it to the actual record schema resolved that fixture failure.
