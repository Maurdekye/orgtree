[package]
name = "orgtree-work-name-codec"
version = "0.1.0"
edition = "2021"
publish = false
description = "Dependency-free canonical work-name wire codec and legacy namespace preflight"
license = "MIT"

[lib]
path = "src/lib.rs"

[dependencies]
