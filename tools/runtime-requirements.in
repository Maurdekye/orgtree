# Application dependencies copied from v1 requirements.txt (MIT, Maurdekye).
# Scope excludes optional development browser tools; runtime manifest records
# exact resolved versions and artifact hashes inside the packaged directory.
fastapi>=0.110
uvicorn>=0.27
websockets>=12
pydantic>=2.6
starlette>=0.36
typing_extensions>=4.8
httpx>=0.27
Pillow>=10
psutil>=7
# PG-0 (PYPG): the PostgreSQL driver for ORGTREE_STORE=postgres (LGPL-3.0;
# engine/backend/orgtree/DEPENDENCIES.md). The binary wheel bundles libpq.
psycopg[binary]>=3.2,<3.4
